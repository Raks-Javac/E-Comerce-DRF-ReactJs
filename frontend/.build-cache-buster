# Force deployment refresh - $(date)
# This file forces Render to rebuild from scratch

BUILD_ID=$(date +%s)
echo "Build ID: $BUILD_ID"

# Add timestamp to package.json to force cache invalidation
echo "Forcing fresh build..."
